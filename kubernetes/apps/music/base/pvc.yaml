---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  namespace: music
  name: music-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 30Gi
