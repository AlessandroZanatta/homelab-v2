---
apiVersion: seaweed.seaweedfs.com/v1
kind: S3Identity
metadata:
  name: tasks
spec:
  seaweedRef:
    name: backups
    namespace: backups
---
apiVersion: seaweed.seaweedfs.com/v1
kind: S3Credentials
metadata:
  name: tasks
spec:
  seaweedRef:
    name: backups
    namespace: backups
  identityRef:
    name: tasks
  secretRef:
    name: tasks-s3-credentials
---
apiVersion: seaweed.seaweedfs.com/v1
kind: S3Policy
metadata:
  name: tasks
spec:
  seaweedRef:
    name: backups
    namespace: backups
  policyDocument: |
    {
      "Version": "2012-10-17",
      "Statement": [
        {
          "Effect": "Allow",
          "Action": [
            "s3:ListBucket"
          ],
          "Resource": [
            "arn:aws:s3:::cnpg"
          ]
        },
        {
          "Effect": "Allow",
          "Action": [
            "s3:GetObject",
            "s3:PutObject",
            "s3:AbortMultipartUpload"
          ],
          "Resource": [
            "arn:aws:s3:::cnpg/tasks/*"
          ]
        }
      ]
    }
---
apiVersion: seaweed.seaweedfs.com/v1
kind: S3PolicyBinding
metadata:
  name: tasks
spec:
  seaweedRef:
    name: backups
    namespace: backups
  policyRef:
    name: tasks
  subjects:
    - kind: S3Identity
      name: tasks
