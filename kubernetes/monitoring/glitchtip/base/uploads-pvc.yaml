---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: glitchtip
  name: uploads-pvc
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
