{
  "auths": {
    "registry.kalexlab.xyz": {
      "auth": "{{ (registry_username + ':' +  registry_password)|b64encode }}"
    },
    "https://index.docker.io/v1/": {
      "auth": "{{ (dockerhub_username + ':' + dockerhub_password)|b64encode }}"
    }
  }{% if ansible_environment != 'prod' %},
  "insecure-registries": ["registry.kalexlab.xyz"]{% endif %}
}
