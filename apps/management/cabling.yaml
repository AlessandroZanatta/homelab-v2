---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cabling
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/AlessandroZanatta/homelab-v2.git
      targetRevision: master
      path: kubernetes/management/cabling
  destination:
    name: in-cluster
    namespace: cabling
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
