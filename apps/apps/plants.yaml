---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plants
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/AlessandroZanatta/homelab-v2.git
      targetRevision: master
      path: kubernetes/apps/plants
  destination:
    name: in-cluster
    namespace: plants
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
