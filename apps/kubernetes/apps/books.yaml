---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: books
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters: {}
  template:
    metadata:
      name: books
    spec:
      project: default
      sources:
        - repoURL: "https://github.com/AlessandroZanatta/homelab-v2.git"
          targetRevision: master
          path: "kubernetes/apps/books/overlays/{{ .metadata.labels.env }}"
      destination:
        server: "{{ .server }}"
        namespace: books
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
