---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: expo-updates
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters: {}
  template:
    metadata:
      name: expo-updates
    spec:
      project: default
      sources:
        - repoURL: "https://github.com/AlessandroZanatta/homelab-v2.git"
          targetRevision: master
          path: "kubernetes/management/expo-updates/overlays/{{ .metadata.labels.env }}"
      destination:
        server: "{{ .server }}"
        namespace: expo-updates
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
