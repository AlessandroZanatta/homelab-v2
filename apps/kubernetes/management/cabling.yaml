---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cabling
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters: {}
  template:
    metadata:
      name: cabling
    spec:
      project: default
      sources:
        - repoURL: "https://github.com/AlessandroZanatta/homelab-v2.git"
          targetRevision: master
          path: "kubernetes/management/cabling/overlays/{{ .metadata.labels.env }}"
      destination:
        server: "{{ .server }}"
        namespace: cabling
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
